name: elastic_sheet
description: Elastic expand/collapse surface for Flutter with realistic showcase examples.
version: 0.1.0
homepage: https://github.com/Abdualsslam/elastic_sheet
repository: https://github.com/Abdualsslam/elastic_sheet
issue_tracker: https://github.com/Abdualsslam/elastic_sheet/issues

environment:
  sdk: ^3.10.4

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
